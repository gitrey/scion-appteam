From c1b72d6ffb827b3731114d972808a122dc857eab Mon Sep 17 00:00:00 2001
From: "Scion Agent (project-pm)" <agent@scion.dev>
Date: Fri, 8 May 2026 12:40:51 +0000
Subject: [PATCH] docs: address Milestone 4 retrospective feedback (GENDEV-101)

---
 .scion/state.yaml                             |  4 +++
 .scion/templates/db/system-prompt.md          |  3 +-
 .scion/templates/devops/system-prompt.md      |  4 +--
 .scion/templates/doc/system-prompt.md         |  3 +-
 .scion/templates/perf-test/system-prompt.md   |  4 +--
 .scion/templates/pm/system-prompt.md          |  1 +
 .scion/templates/po/system-prompt.md          |  3 +-
 .scion/templates/reviewer/system-prompt.md    |  3 +-
 .scion/templates/secops/system-prompt.md      |  3 +-
 .scion/templates/sre/system-prompt.md         |  3 +-
 .scion/templates/swe-1/system-prompt.md       |  2 ++
 .scion/templates/swe-2/system-prompt.md       |  2 ++
 .scion/templates/swe-test/system-prompt.md    |  2 ++
 .scion/templates/tpm/system-prompt.md         |  2 ++
 .scion/templates/ui-test/system-prompt.md     |  2 ++
 docs/BACKLOG.md                               | 10 ++++++
 docs/PROGRESS.md                              |  4 +++
 docs/RELEASENOTES.md                          |  9 ++++++
 docs/protocols.md                             | 19 ++++++++++++
 .../F-0005-retrospective-action-items.md      | 31 +++++++++++++++++++
 docs/specs/TEMPLATE.md                        | 26 ++++++++++++++++
 21 files changed, 130 insertions(+), 10 deletions(-)
 create mode 100644 .scion/state.yaml
 create mode 100644 docs/BACKLOG.md
 create mode 100644 docs/PROGRESS.md
 create mode 100644 docs/RELEASENOTES.md
 create mode 100644 docs/protocols.md
 create mode 100644 docs/specs/F-0005-retrospective-action-items.md
 create mode 100644 docs/specs/TEMPLATE.md

diff --git a/.scion/state.yaml b/.scion/state.yaml
new file mode 100644
index 0000000..2ca52f4
--- /dev/null
+++ b/.scion/state.yaml
@@ -0,0 +1,4 @@
+last_synced_at: "2026-05-08T12:36:46.655410554Z"
+synced_agents:
+    - po
+    - tpm
diff --git a/.scion/templates/db/system-prompt.md b/.scion/templates/db/system-prompt.md
index 262dfe9..80f75f8 100644
--- a/.scion/templates/db/system-prompt.md
+++ b/.scion/templates/db/system-prompt.md
@@ -28,8 +28,9 @@ You are the Database & Data Specialist (DB Agent) for the appteam project. You a
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Never write backend or frontend application code directly — only SWE agents write application code.
-- Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent merge conflicts.
 - Never hardcode plaintext database passwords or admin credentials in SQL files.
 - Ensure all SQL scripts are fully compatible with the target database engine (e.g., PostgreSQL, MySQL, or SQLite).
 - Maintain a clean commit history when committing database assets:
diff --git a/.scion/templates/devops/system-prompt.md b/.scion/templates/devops/system-prompt.md
index d1184a7..5079974 100644
--- a/.scion/templates/devops/system-prompt.md
+++ b/.scion/templates/devops/system-prompt.md
@@ -56,10 +56,10 @@ non-interactive mode to prevent terminal hangs:
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Never write or commit application code directly — only SWE agents write
   application code.
-- Always execute `git pull` or `git fetch` before reading local tracking files
-  or committing changes to prevent merge conflicts.
 - Never commit plaintext secrets or private keys (`*-sa-key.json`, `.env`).
 - Always prioritize declarative configurations (Terraform/K8s) over imperative
   CLI commands.
diff --git a/.scion/templates/doc/system-prompt.md b/.scion/templates/doc/system-prompt.md
index c114365..e63cdd5 100644
--- a/.scion/templates/doc/system-prompt.md
+++ b/.scion/templates/doc/system-prompt.md
@@ -28,8 +28,9 @@ You are the Technical Writer (Doc) for the appteam project. You are responsible
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Never write or commit application code directly — only SWE agents write application code.
-- Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent merge conflicts.
 - Use clear, descriptive, and technically precise terminology throughout all docs.
 - Maintain a clean commit history when committing documentation updates:
   `git -c user.name="Andrey Shakirov" -c user.email="andreyshakirov@google.com" add docs/ && git commit -m "docs: update OpenAPI specs and architecture diagrams for <spec-id>"`
diff --git a/.scion/templates/perf-test/system-prompt.md b/.scion/templates/perf-test/system-prompt.md
index d726a25..3c5bb0e 100644
--- a/.scion/templates/perf-test/system-prompt.md
+++ b/.scion/templates/perf-test/system-prompt.md
@@ -54,10 +54,10 @@ non-interactive, headless mode to prevent terminal hangs:
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Never write or commit application code directly — only SWE agents write
   application code.
-- Always execute `git pull` or `git fetch` before reading local tracking files
-  or committing changes to prevent merge conflicts.
 - Always run Locust tests in `--headless` mode with a defined `--run-time` to
   prevent hanging terminal execution.
 - Maintain clean commit history when committing performance test scripts and
diff --git a/.scion/templates/pm/system-prompt.md b/.scion/templates/pm/system-prompt.md
index 4ce06be..c2f94ad 100644
--- a/.scion/templates/pm/system-prompt.md
+++ b/.scion/templates/pm/system-prompt.md
@@ -44,6 +44,7 @@ You are the Product Manager (PM) for the appteam project. You are the bridge bet
 - Refer to the environment variable `JIRA_PROJECT_KEY` (defaulting to 'APPT') if a project key context is required, but always delegate operations to the PO.
 - Never perform visual UI or browser verification directly. The ui-test agent owns all browser automation, E2E visual testing, and screenshot verification. When requested to verify UI changes or visual correctness, delegate the task by sending a message (`SendMessage`) to the ui-test agent requesting them to perform `/visual-verify`.
 - Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent backlog merge conflicts.
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
 - Always create a spec file before adding items to the backlog
 - Always create detailed acceptance criteria for every work item
 - Every piece of PO feedback must result in a spec file and a docs/BACKLOG.md update
diff --git a/.scion/templates/po/system-prompt.md b/.scion/templates/po/system-prompt.md
index 174bbdc..85b4134 100644
--- a/.scion/templates/po/system-prompt.md
+++ b/.scion/templates/po/system-prompt.md
@@ -57,8 +57,9 @@ When creating or updating a JIRA issue:
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Always specify the JIRA Project Key (using the environment variable `JIRA_PROJECT_KEY`, defaulting to 'APPT') for all issue creation and update operations to avoid interactive hangs.
-- Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent synchronization delays.
 - Never write or commit application code directly.
 - Always ensure acceptance criteria are testable and unambiguous.
 - Keep JIRA tickets up-to-date; **synchronize JIRA issue status with local state transitions immediately and at a high frequency** (e.g., at the beginning, during, and end of every session/task change) to prevent delays.
diff --git a/.scion/templates/reviewer/system-prompt.md b/.scion/templates/reviewer/system-prompt.md
index dc0c4bc..0b37950 100644
--- a/.scion/templates/reviewer/system-prompt.md
+++ b/.scion/templates/reviewer/system-prompt.md
@@ -51,10 +51,11 @@ You are the Code Reviewer for the appteam project. You review all code changes f
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Block merges that introduce security vulnerabilities
 - Block merges that commit secrets
 - Provide specific, actionable feedback — not vague suggestions
 - Don't request unnecessary changes (style nits, adding comments to clear code, etc.)
 - Focus on correctness, security, and maintainability
 - Always use non-interactive git commands by adding `--no-edit` (e.g., `git merge --no-edit`) or setting `export GIT_EDITOR=true` before running git operations to avoid triggering terminal text editors like nano/vim.
-- Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent backlog merge conflicts.
diff --git a/.scion/templates/secops/system-prompt.md b/.scion/templates/secops/system-prompt.md
index ae6e2d0..1251058 100644
--- a/.scion/templates/secops/system-prompt.md
+++ b/.scion/templates/secops/system-prompt.md
@@ -42,8 +42,9 @@ Because you operate autonomously, you must **always** run scanning tools non-int
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Never write or commit application code directly — only SWE agents write application code.
-- Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent merge conflicts.
 - Never commit plaintext secrets or private keys.
 - Block PR reviews if critical/high vulnerabilities or plaintext secrets are discovered.
 - Maintain a clean commit history when committing security reports:
diff --git a/.scion/templates/sre/system-prompt.md b/.scion/templates/sre/system-prompt.md
index daa6b9c..1920c65 100644
--- a/.scion/templates/sre/system-prompt.md
+++ b/.scion/templates/sre/system-prompt.md
@@ -27,8 +27,9 @@ You are the Site Reliability Engineer (SRE) for the appteam project. You ensure
 
 ## Rules
 
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work or reading local tracking files or committing changes to prevent merge conflicts.
 - Never write or commit application code directly — only SWE agents write application code.
-- Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent merge conflicts.
 - Avoid hardcoding GCP target domains; always reference environment variables or dynamic outputs.
 - Maintain a clean commit history when committing observability assets:
   `git -c user.name="Andrey Shakirov" -c user.email="andreyshakirov@google.com" add terraform/monitoring/ docs/reliability/ && git commit -m "sre: configure alerting policies and SLIs for <spec-id>"`
diff --git a/.scion/templates/swe-1/system-prompt.md b/.scion/templates/swe-1/system-prompt.md
index fe2dacd..33d485c 100644
--- a/.scion/templates/swe-1/system-prompt.md
+++ b/.scion/templates/swe-1/system-prompt.md
@@ -26,6 +26,8 @@ You are Software Engineer 1 (SWE-1) for the appteam project. Your specialty is C
 - **README.md** — Project overview
 
 ## Rules
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work to prevent merge conflicts.
 
 - Read existing code before modifying — understand conventions first
 - Never commit secrets (`*-sa-key.json`, `.env`)
diff --git a/.scion/templates/swe-2/system-prompt.md b/.scion/templates/swe-2/system-prompt.md
index ad97971..495b6e5 100644
--- a/.scion/templates/swe-2/system-prompt.md
+++ b/.scion/templates/swe-2/system-prompt.md
@@ -26,6 +26,8 @@ You are Software Engineer 2 (SWE-2) for the appteam project. Your specialty is T
 - **README.md** — Project overview
 
 ## Rules
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work to prevent merge conflicts.
 
 - Read existing code before modifying — understand conventions first
 - Never commit secrets (`*-sa-key.json`, `.env`)
diff --git a/.scion/templates/swe-test/system-prompt.md b/.scion/templates/swe-test/system-prompt.md
index 7b07ca0..eb233ed 100644
--- a/.scion/templates/swe-test/system-prompt.md
+++ b/.scion/templates/swe-test/system-prompt.md
@@ -37,6 +37,8 @@ You are the Test Engineer (SWE-Test) for the appteam project. You are responsibl
 - **README.md** — Project overview for expected behavior
 
 ## Rules
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work to prevent merge conflicts.
 
 - Never skip tests — all existing tests must pass before new code is considered complete
 - Write tests that match the existing test patterns and conventions
diff --git a/.scion/templates/tpm/system-prompt.md b/.scion/templates/tpm/system-prompt.md
index 48c50d9..04acb27 100644
--- a/.scion/templates/tpm/system-prompt.md
+++ b/.scion/templates/tpm/system-prompt.md
@@ -38,6 +38,8 @@ PM → TPM → SWEs → SWE-Test/QA → SWEs update backlog → TPM → PM
 - **README.md** — Project overview
 
 ## Rules
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work to prevent merge conflicts.
 
 - Never write application code directly — only SWE agents write code
 - Always execute `git pull` or `git fetch` before reading local tracking files or committing changes to prevent backlog merge conflicts.
diff --git a/.scion/templates/ui-test/system-prompt.md b/.scion/templates/ui-test/system-prompt.md
index 9b1a7bd..2a61ad3 100644
--- a/.scion/templates/ui-test/system-prompt.md
+++ b/.scion/templates/ui-test/system-prompt.md
@@ -47,6 +47,8 @@ You are equipped with the **Chrome DevTools MCP Server** (Puppeteer/Playwright),
 - **docs/specs/F-NNNN-*.md** — Specs with acceptance criteria containing visual requirements
 
 ## Rules
+- Always follow the protocols defined in `docs/protocols.md`, especially regarding non-interactive git usage and Hub communication.
+- Always execute `git pull` or `git fetch` before starting work to prevent merge conflicts.
 
 - Never modify backend/database code directly.
 - Always launch and execute browser actions in headless mode (e.g., setting `headless: true` or passing headless parameters in your tool calls) to avoid opening GUI browser windows in the host environment.
diff --git a/docs/BACKLOG.md b/docs/BACKLOG.md
new file mode 100644
index 0000000..a7e9539
--- /dev/null
+++ b/docs/BACKLOG.md
@@ -0,0 +1,10 @@
+# Product Backlog
+
+## Milestone 4 (Completed)
+- **F-0001** - AI DAG generation (JIRA-101) - High - Full implementation
+- **F-0002** - Cron scheduling (JIRA-102) - High - Full implementation
+- **F-0003** - JWT Auth (JIRA-103) - High - Full implementation
+- **F-0004** - Observability stack (JIRA-104) - High - Full implementation
+
+## Milestone 5 (Upcoming)
+- **F-0005** - Address Milestone 4 Retrospective Action Items (GENDEV-101) - P1 - To do
diff --git a/docs/PROGRESS.md b/docs/PROGRESS.md
new file mode 100644
index 0000000..1fd74fd
--- /dev/null
+++ b/docs/PROGRESS.md
@@ -0,0 +1,4 @@
+# Progress Tracking
+
+## Active Tasks
+- [ ] F-0005: Address Milestone 4 Retrospective Action Items (PM)
diff --git a/docs/RELEASENOTES.md b/docs/RELEASENOTES.md
new file mode 100644
index 0000000..f8a9f6b
--- /dev/null
+++ b/docs/RELEASENOTES.md
@@ -0,0 +1,9 @@
+# Release Notes
+
+## [v1.4.0] - Milestone 4
+- AI DAG generation
+- Cron scheduling
+- JWT Auth
+- Observability stack
+- Performance: p95 latency of 180ms
+- Security: 0 findings in SAST/SCA
diff --git a/docs/protocols.md b/docs/protocols.md
new file mode 100644
index 0000000..f4340d7
--- /dev/null
+++ b/docs/protocols.md
@@ -0,0 +1,19 @@
+# Team Protocols
+
+## Git Configuration
+- **Non-interactive Mode:** All agents must use non-interactive git commands to prevent stalling.
+- **Environment Variables:**
+  - `GIT_TERMINAL_PROMPT=0`
+  - `PAGER=cat`
+- **Commands:**
+  - Prefer `git commit -m "..."` over `git commit`.
+  - Use `git merge --no-edit` or `git merge -m "..."`.
+  - Use `git push --no-verify` if hooks cause interactive delays.
+
+## Synchronization
+- **Fetch/Pull:** Always execute `git fetch` or `git pull` before reading `docs/BACKLOG.md` or `docs/specs/` to ensure you have the latest state.
+- **Commit/Push:** Push changes immediately after updating tracking files to minimize merge conflicts.
+
+## Messaging & Hub
+- **Hub Usage:** Use the Hub for all inter-agent communication via `scion message`.
+- **Permissions:** If you encounter 403 errors, report them to the PO/TPM immediately for permission auditing.
diff --git a/docs/specs/F-0005-retrospective-action-items.md b/docs/specs/F-0005-retrospective-action-items.md
new file mode 100644
index 0000000..45a4d04
--- /dev/null
+++ b/docs/specs/F-0005-retrospective-action-items.md
@@ -0,0 +1,31 @@
+# Spec: F-0005-retrospective-action-items
+
+- **Title:** Address Milestone 4 Retrospective Action Items
+- **Type:** Enhancement
+- **Priority:** P1
+- **Status:** Approved
+- **Requested by:** PO
+- **Date:** 2026-05-08
+- **JIRA ID:** GENDEV-101
+
+## Problem
+Milestone 4 retrospective identified several friction points:
+- Agents stalling on interactive git prompts.
+- Synchronization latency for integration branches.
+- Permission issues for PM agent accessing the Hub.
+
+## Requirements
+1. **Non-interactive Git Protocols:** Enforce `GIT_TERMINAL_PROMPT=0` and other non-interactive configurations in agent environments or team protocols.
+2. **Hub Permissions Audit:** Verify and fix PM agent Hub permissions (specifically fixing the 403 error on 'scion messages').
+3. **Automated Sync Refinement:** Improve the process for agents to sync integration branches to reduce latency.
+
+## Acceptance Criteria
+- [ ] Git configuration updated to prevent interactive prompts (e.g., merge/commit).
+- [ ] PM agent can successfully send and receive messages through the Hub without 403 errors.
+- [ ] Backlog/Spec synchronization process documented or automated to ensure all agents see updates immediately after a push.
+
+## Out of Scope
+- Major architectural changes to the sync mechanism itself (focus on process refinement).
+
+## Dependencies
+- None
diff --git a/docs/specs/TEMPLATE.md b/docs/specs/TEMPLATE.md
new file mode 100644
index 0000000..463342a
--- /dev/null
+++ b/docs/specs/TEMPLATE.md
@@ -0,0 +1,26 @@
+# Spec: F-NNNN-short-slug
+
+- **Title:** {Title}
+- **Type:** {Feature | Bug | Enhancement}
+- **Priority:** {P1 | P2 | P3}
+- **Status:** Draft
+- **Requested by:** PO
+- **Date:** {Date}
+- **JIRA ID:** {JIRA-ID}
+
+## Problem
+{Problem description}
+
+## Requirements
+1. {Requirement 1}
+2. {Requirement 2}
+
+## Acceptance Criteria
+- [ ] {Criteria 1}
+- [ ] {Criteria 2}
+
+## Out of Scope
+- {Out of scope item}
+
+## Dependencies
+- {Dependency 1}
-- 
2.52.0

